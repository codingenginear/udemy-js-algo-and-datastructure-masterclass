// Analyzing Performance of Arrays and Objects

const array = [];

// Removing or Adding to the end is O(1) Performance
array.push('1');
array.push('2');

// Removing or adding from beginning is O(n) Performance
const variable = array.unshift();

// Accessing is O(1) Performance
const indexZero = array[0];

